[project]
name = "code_golf"
version = "0.1.0"
requires-python = ">=3.10"
description = "ARC DSL auto-solver and pipeline"
dependencies = [
  "numpy",
  "pandas",
  "matplotlib",
]

[tool.setuptools.packages.find]
where = ["src"]
